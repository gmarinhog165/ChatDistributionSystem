#To test SA and SC

run the bash scrpts inside the respective folder and attempt to create a topic with:
echo "CREATE_TOPIC gaja~ss joao" | nc 127.0.0.1 9999
(cria o topico no SA 10000 (a porta que recebe clientes é 10000-1))
